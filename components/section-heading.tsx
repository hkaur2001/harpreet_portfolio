export function SectionHeading({ eyebrow, title, body }: { eyebrow: string; title: string; body?: string }) { return <div className="max-w-3xl"><p className="font-mono text-xs uppercase tracking-[0.18em] text-[var(--signal)]">{eyebrow}</p><h2 className="mt-4 text-balance text-3xl font-semibold tracking-[-0.035em] sm:text-4xl md:text-5xl">{title}</h2>{body ? <p className="mt-5 text-lg leading-8 text-[var(--muted)]">{body}</p> : null}</div>; }
