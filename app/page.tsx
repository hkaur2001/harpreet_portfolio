import Link from "next/link";
import { SectionHeading } from "@/components/section-heading";
import { capabilities, engineeringSurface, experienceHighlights, impact, labs, principles, site } from "@/lib/site";

const sentinelSignals = [
  "Adaptive tool calling",
  "OpenAI Responses API",
  "MCP tool server",
  "Deterministic policy",
  "Human approval",
  "Prompt-injection defense",
  "Trace + cost telemetry",
  "100-case safety suite",
];

export default function Home() {
  return (
    <main>
      <section className="grid-field border-b border-[var(--line)]">
        <div className="mx-auto max-w-7xl px-5 py-20 md:px-8 md:py-28 lg:py-32">
          <div className="reveal max-w-6xl">
            <div className="mb-8 flex flex-wrap items-center gap-2 text-xs">
              <span className="rounded-full border border-[var(--line)] bg-[var(--surface)] px-3 py-1.5">New York, NY</span>
              <span className="rounded-full border border-[var(--line)] bg-[var(--surface)] px-3 py-1.5">Software engineering</span>
              <span className="rounded-full border border-[var(--line)] bg-[var(--surface)] px-3 py-1.5">Applied AI</span>
              <span className="rounded-full border border-[var(--line)] bg-[var(--surface)] px-3 py-1.5">Forward deployed systems</span>
            </div>
            <h1 className="max-w-5xl text-balance text-5xl font-semibold tracking-[-0.055em] sm:text-6xl md:text-7xl lg:text-[84px] lg:leading-[0.98]">I turn ambiguous problems into production software.</h1>
            <p className="mt-8 max-w-3xl text-balance text-lg leading-8 text-[var(--muted)] md:text-xl">I work across product UI, backend services, AI systems, data integrations, evaluation, and production operations—owning the path from understanding the problem to shipping something people can rely on.</p>
            <div className="mt-9 flex flex-wrap gap-3"><Link href="/projects/sentinel" className="btn-primary rounded-full px-5">Run flagship project →</Link><Link href="#experience" className="btn-secondary">See production experience</Link></div>
          </div>
          <div className="mt-20 grid gap-px overflow-hidden rounded-3xl border border-[var(--line)] bg-[var(--line)] md:grid-cols-4">{impact.map((item) => <div key={item.label} className="bg-[var(--surface)] p-6 md:p-7"><p className="text-3xl font-semibold tracking-[-0.04em]">{item.value}</p><p className="mt-2 text-sm font-medium">{item.label}</p><p className="mt-4 text-xs leading-5 text-[var(--muted)]">{item.detail}</p></div>)}</div>
        </div>
      </section>

      <section className="border-b border-[var(--line)]"><div className="mx-auto max-w-7xl px-5 py-20 md:px-8 md:py-28"><SectionHeading eyebrow="Engineering range" title="The interesting work happens between layers." body="I want the portfolio to show the same range I bring to a real system: interface, APIs, data, AI behavior, security, reliability, economics, and deployment." /><div className="mt-12 grid gap-4 md:grid-cols-2 lg:grid-cols-4">{engineeringSurface.map((item) => <article key={item.label} className="rounded-3xl border border-[var(--line)] bg-[var(--surface)] p-6"><p className="font-mono text-[11px] uppercase tracking-[0.14em] text-[var(--signal)]">{item.label}</p><h3 className="mt-4 text-xl font-semibold tracking-[-0.03em]">{item.value}</h3><p className="mt-4 text-sm leading-6 text-[var(--muted)]">{item.proof}</p></article>)}</div><div className="mt-6 grid gap-px overflow-hidden rounded-3xl border border-[var(--line)] bg-[var(--line)] md:grid-cols-2">{capabilities.map((cap) => <article key={cap.title} className="bg-[var(--surface)] p-7 md:p-9"><h3 className="text-xl font-semibold tracking-[-0.025em]">{cap.title}</h3><p className="mt-4 max-w-xl text-sm leading-7 text-[var(--muted)]">{cap.body}</p><div className="mt-6 flex flex-wrap gap-2">{cap.proof.map((item) => <span key={item} className="rounded-full bg-[var(--soft)] px-3 py-1.5 text-xs">{item}</span>)}</div></article>)}</div></div></section>

      <section id="builds" className="border-b border-[var(--line)]"><div className="mx-auto max-w-7xl px-5 py-20 md:px-8 md:py-28"><div className="grid gap-10 lg:grid-cols-[0.72fr_1.28fr] lg:items-start"><div><p className="font-mono text-xs uppercase tracking-[0.16em] text-[var(--signal)]">Flagship · Sentinel</p><h2 className="mt-4 text-balance text-4xl font-semibold tracking-[-0.05em] md:text-6xl">An AI production incident-response platform, not a chatbot.</h2><p className="mt-6 text-base leading-7 text-[var(--muted)]">A recruiter can inject a production failure, watch a live model choose investigation tools, inspect evidence and alternative hypotheses, see the policy engine stop the model at the remediation boundary, approve a simulated recovery, and get a generated postmortem.</p><div className="mt-7 flex flex-wrap gap-2">{sentinelSignals.map((signal) => <span key={signal} className="rounded-full border border-[var(--line)] bg-[var(--surface)] px-3 py-1.5 text-xs">{signal}</span>)}</div><div className="mt-8 flex flex-wrap gap-3"><Link href="/projects/sentinel" className="btn-primary rounded-full px-5">Investigate an incident →</Link><a href="https://github.com/hkaur2001/harpreet_portfolio/tree/main/sentinel" target="_blank" rel="noreferrer" className="btn-secondary">Inspect architecture ↗</a></div></div><div className="overflow-hidden rounded-[2rem] border border-[var(--line)] bg-[var(--surface)]"><div className="border-b border-[var(--line)] bg-[var(--soft)] px-6 py-4"><div className="flex items-center justify-between gap-3"><div><p className="font-mono text-[10px] uppercase tracking-[0.14em] text-[var(--signal)]">INC-1842 · SEV-1</p><p className="mt-1 font-semibold">Payment API errors: 0.2% → 17%</p></div><span className="rounded-full bg-[var(--surface)] px-3 py-1 text-[10px] font-semibold">LIVE TOOL LOOP</span></div></div><div className="p-6"><div className="grid gap-2">{[["01","query_metrics()","DB pool utilization reached 98%"],["02","search_logs()","Connection acquisition timeouts detected"],["03","get_recent_deployments()","deploy-827 landed minutes before incident"],["04","get_git_changes()","worker concurrency changed 20 → 60"],["05","search_runbooks()","DB-014 matches the failure signature"]].map(([n, tool, detail]) => <div key={n} className="grid grid-cols-[36px_1fr] gap-3 rounded-xl border border-[var(--line)] bg-[var(--bg)] p-3"><span className="font-mono text-[10px] text-[var(--signal)]">{n}</span><div><code className="text-xs font-semibold">{tool}</code><p className="mt-1 text-xs text-[var(--muted)]">{detail}</p></div></div>)}</div><div className="mt-5 rounded-2xl bg-[var(--ink)] p-5 text-white"><p className="text-[10px] uppercase tracking-[0.12em] text-white/60">Policy result</p><p className="mt-2 text-lg font-semibold">Rollback recommended · human approval required</p><p className="mt-2 text-xs leading-5 text-white/65">The model can recommend the action. It cannot grant itself permission to execute it.</p></div></div></div></div></div></section>

      <section className="border-b border-[var(--line)]"><div className="mx-auto max-w-7xl px-5 py-20 md:px-8 md:py-28"><SectionHeading eyebrow="Other public builds" title="Different problems, different engineering surfaces." body="I deliberately avoid filling the site with five versions of the same agent. Each project exists to prove a distinct capability." /><div className="mt-12 grid gap-5 lg:grid-cols-2"><article className="rounded-3xl border border-[var(--line)] bg-[var(--surface)] p-7 md:p-9"><p className="font-mono text-xs uppercase tracking-[0.14em] text-[var(--signal)]">LIVE DATA PRODUCT</p><h3 className="mt-5 text-3xl font-semibold tracking-[-0.04em]">AI Policy Radar</h3><p className="mt-4 text-sm leading-7 text-[var(--muted)]">A live Federal Register monitoring product that proves external API integration, server rendering, caching, normalization, source provenance, and graceful upstream failure handling without relying on an LLM.</p><div className="mt-6 flex flex-wrap gap-2">{["Next.js", "Public API", "Caching", "Normalization", "Provenance", "Failure handling"].map((x) => <span key={x} className="rounded-full bg-[var(--soft)] px-3 py-1.5 text-xs">{x}</span>)}</div><Link href="/projects/policy-radar" className="btn-secondary mt-8">Open live data product →</Link></article><article className="rounded-3xl border border-[var(--line)] bg-[var(--surface)] p-7 md:p-9"><p className="font-mono text-xs uppercase tracking-[0.14em] text-[var(--signal)]">SYSTEM BEHAVIOR LAB</p><h3 className="mt-5 text-3xl font-semibold tracking-[-0.04em]">Agent Systems Lab</h3><p className="mt-4 text-sm leading-7 text-[var(--muted)]">Server-executed workflows for permission-aware retrieval, deployment discovery, and reliability behavior. Each run exposes tool purpose, evidence, decision logic, guardrails, and output metrics rather than hiding everything behind a chat bubble.</p><div className="mt-6 flex flex-wrap gap-2">{["RAG", "Authorization", "Tool orchestration", "ROI logic", "Observability", "Evals"].map((x) => <span key={x} className="rounded-full bg-[var(--soft)] px-3 py-1.5 text-xs">{x}</span>)}</div><Link href="/labs" className="btn-secondary mt-8">Run the workflows →</Link></article></div></div></section>

      <section id="experience" className="border-b border-[var(--line)]"><div className="mx-auto max-w-7xl px-5 py-20 md:px-8 md:py-28"><SectionHeading eyebrow="Production experience" title="Enterprise engineering at S&P Global." body="This is where the public projects connect back to real production judgment: integration complexity, permissions, reliability, organizational scale, and shipping across technical and business boundaries." /><div className="mt-12">{experienceHighlights.map((x) => <article key={x.company} className="grid gap-8 rounded-3xl border border-[var(--line)] bg-[var(--surface)] p-7 md:grid-cols-[0.55fr_1.45fr] md:p-9"><div><p className="text-2xl font-semibold tracking-[-0.03em]">{x.company}</p><p className="mt-2 text-sm text-[var(--muted)]">{x.role}</p></div><div><p className="max-w-3xl text-lg leading-8">{x.body}</p><div className="mt-7 grid gap-3">{x.bullets.map((b) => <div key={b} className="flex gap-4 border-t border-[var(--line)] pt-4 text-sm leading-6 text-[var(--muted)]"><span className="font-mono text-[var(--signal)]">↳</span><p>{b}</p></div>)}</div></div></article>)}<div className="mt-4 flex flex-wrap items-center justify-between gap-3 rounded-2xl border border-[var(--line)] bg-[var(--surface)] px-6 py-4 text-sm"><span><strong>Princeton University</strong> · B.S.E. Computer Science</span><span className="text-[var(--muted)]">Resume available directly to recruiting teams on request.</span></div></div></div></section>

      <section className="border-b border-[var(--line)]"><div className="mx-auto max-w-7xl px-5 py-20 md:px-8 md:py-28"><div className="flex flex-col justify-between gap-7 md:flex-row md:items-end"><SectionHeading eyebrow="Working labs" title="Small proofs for specific engineering decisions." body="Sentinel is the deep flagship. These narrower workflows make individual capabilities easy to inspect and discuss." /><Link href="/labs" className="shrink-0 text-sm font-semibold underline decoration-[var(--line)] underline-offset-4">Open all labs →</Link></div><div className="mt-12 grid gap-4 lg:grid-cols-3">{labs.map((lab) => <Link href={`/labs#${lab.slug}`} key={lab.slug} className="group rounded-3xl border border-[var(--line)] bg-[var(--surface)] p-7 transition hover:-translate-y-1 hover:border-[var(--ink)]"><p className="font-mono text-[11px] uppercase tracking-[0.14em] text-[var(--signal)]">{lab.kicker}</p><h3 className="mt-5 text-2xl font-semibold tracking-[-0.03em]">{lab.title}</h3><p className="mt-4 text-sm leading-7 text-[var(--muted)]">{lab.summary}</p><div className="mt-7 flex flex-wrap gap-2">{lab.skills.slice(0, 4).map((s) => <span key={s} className="rounded-full bg-[var(--soft)] px-2.5 py-1 text-[11px]">{s}</span>)}</div><p className="mt-8 text-sm font-semibold">Inspect workflow <span className="inline-block transition group-hover:translate-x-1">→</span></p></Link>)}</div></div></section>

      <section id="thinking" className="border-b border-[var(--line)]"><div className="mx-auto max-w-7xl px-5 py-20 md:px-8 md:py-28"><SectionHeading eyebrow="How I think" title="Engineering principles that travel across products, AI, and infrastructure." /><div className="mt-12 divide-y divide-[var(--line)] border-y border-[var(--line)]">{principles.map(([title, body], i) => <div key={title} className="grid gap-4 py-7 md:grid-cols-[80px_0.7fr_1.3fr] md:items-start"><span className="font-mono text-xs text-[var(--muted)]">0{i + 1}</span><h3 className="font-semibold">{title}</h3><p className="max-w-2xl text-sm leading-7 text-[var(--muted)]">{body}</p></div>)}</div></div></section>

      <section><div className="mx-auto max-w-7xl px-5 py-20 md:px-8 md:py-28"><div className="rounded-[2rem] bg-[var(--ink)] px-7 py-12 text-white md:px-12 md:py-16"><p className="font-mono text-xs uppercase tracking-[0.16em] text-white/60">Next problem</p><h2 className="mt-5 max-w-4xl text-balance text-4xl font-semibold tracking-[-0.045em] md:text-6xl">Give me the workflow that is messy enough to need judgment.</h2><p className="mt-6 max-w-2xl text-base leading-7 text-white/70">I’m interested in engineering roles where the problem starts ambiguous and the work spans users, architecture, code, data, AI behavior, security, evaluation, and operating the thing after it ships.</p><div className="mt-8 flex flex-wrap gap-3"><a href={`mailto:${site.email}`} className="inline-flex items-center justify-center rounded-full bg-white px-5 py-3 text-sm font-semibold text-black transition hover:opacity-85">Email me</a><a href={site.linkedin} target="_blank" rel="noreferrer" className="inline-flex items-center justify-center rounded-full border border-white/30 px-5 py-3 text-sm font-semibold text-white transition hover:bg-white hover:text-black">LinkedIn ↗</a></div></div></div></section>
    </main>
  );
}
